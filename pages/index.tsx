import type { NextPage } from 'next';
import { Fragment } from 'react';
import { Header, Navbar } from '../components';

const Home: NextPage = () => {
  return (
    <Fragment>
      {/* <Navbar /> */}
      <Header />
    </Fragment>
  );
};

export default Home;
